{
  "name": "@open-haxball/client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && esbuild src/index.ts --bundle --format=esm --outfile=dist/bundle.js --minify",
    "dev": "esbuild src/index.ts --bundle --format=esm --outfile=dist/bundle.js --servedir=. --serve=8080",
    "start": "npx serve .",
    "preview": "npm run build && npx serve ."
  },
  "dependencies": {
    "@open-haxball/shared": "*"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "typescript": "^5.3.0"
  }
}
